import { css } from 'glamor';
const iOSButtons = css({
  position: 'absolute',
  right: 62,
  top: -30,
  display: 'flex',
  justifyContent: 'flex-end',
  alignItems: 'center',
}).toString();

export default { iOSButtons };
